{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "run",
            "type": "python",
            "request": "launch",
            "program": "${workspaceRoot}/run.py",
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}",
            "justMyCode": true
        }
    ]
}